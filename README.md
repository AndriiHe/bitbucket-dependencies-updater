# Dependencies updater

## Setup
1. Clone the repository
2. Install dependencies `yarn`
3. Copy `.env.sample` file -> `.env` and update respective variables
   3.1. To get `BITBUCKET_ACCESS_TOKEN` you need go to repository -> repository settings - security -> access tokens -> create repository access token
4. Run `yarn start`